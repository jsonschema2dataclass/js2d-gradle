rootProject.name = "Jsonschema2dataclass Android AGP7 sample"

include ':app'
include ':lib'
include ':lib2'

if (settings.ext.has("org.jsonschema2pojo.local") &&
        settings.ext["org.jsonschema2pojo.local"].toString().toBoolean()) {
    includeBuild("../..")
}
